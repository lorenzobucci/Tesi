version: "3.8"

services:

  mysql:
    image: mysql:latest
    container_name: mysql-resources-management
    environment:
      - MYSQL_ROOT_PASSWORD=admin
      - MYSQL_DATABASE=ResourcesManagement
    healthcheck:
      test: [ "CMD", "mysqladmin" ,"ping", "-h", "localhost" ]
      timeout: 10s
      retries: 20
    networks:
      - resources-management-net

  wildfly:
    build: .
    container_name: wildfly-resources-management
    ports:
      - "9002:9555"
    networks:
      - resources-management-net
    depends_on:
      mysql:
        condition: service_healthy

networks:
  resources-management-net:
    driver: bridge
